import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 50,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 30,

  },

  backButton: {
    position: 'absolute',
    left: 10,
    padding: 10,
  },

  arrow: {
    width: 20,
    height: 20,
  },

  title: {
    fontSize: 22,
  },
})